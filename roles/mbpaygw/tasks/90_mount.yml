---
- name: "register {{ mbpaygw.dirs.production }} chroot dir"
  set_fact: chroot_dir="{{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.chroot }}/{{ mbpaygw.dirs.production }}"

- name: "mount {{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.production }}/public -> {{ chroot_dir }}{{ mbpaygw.dirs.www }}/{{ mbpaygw.dirs.module }}"
  blockinfile:
    path: "/etc/fstab"
    block: |
      # www
      {{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.production }}/public {{ chroot_dir }}{{ mbpaygw.dirs.www }}/{{ mbpaygw.dirs.module }} none defaults,bind 0 0
      # config
      {{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.production }}/config {{ chroot_dir }}{{ billing.dirs.home }}/config none defaults,bind 0 0
      # logs
      {{ billing.dirs.home }}/{{ billing.dirs.logs }}/{{ mbpaygw.dirs.module }} {{ chroot_dir }}{{ billing.dirs.home }}/{{ billing.dirs.logs }} none defaults,bind 0 0
    marker: "# {mark} {{ mbpaygw.dirs.module }} {{ mbpaygw.dirs.production }}"

- name: "register {{ mbpaygw.dirs.testing }} chroot dir"
  set_fact: chroot_dir="{{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.chroot }}/{{ mbpaygw.dirs.testing }}"

- name: "mount {{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.testing }}/public -> {{ chroot_dir }}{{ mbpaygw.dirs.www }}/{{ mbpaygw.dirs.module }}"
  blockinfile:
    path: "/etc/fstab"
    block: |
      # www
      {{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.testing }}/public {{ chroot_dir }}{{ mbpaygw.dirs.www }}/{{ mbpaygw.dirs.module }} none defaults,bind 0 0
      # config
      {{ billing.dirs.home }}/{{ mbpaygw.dirs.module }}/{{ mbpaygw.dirs.testing }}/config {{ chroot_dir }}{{ billing.dirs.home }}/config none defaults,bind 0 0
      # logs
      {{ billing.dirs.home }}/{{ billing.dirs.logs }}/{{ mbpaygw.dirs.module }} {{ chroot_dir }}{{ billing.dirs.home }}/{{ billing.dirs.logs }} none defaults,bind 0 0
    marker: "# {mark} {{ mbpaygw.dirs.module }} {{ mbpaygw.dirs.testing }}"

- name: "re-mount"
  command: "mount -a"
  args:
    warn: no
