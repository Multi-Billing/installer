---
- name: "detect php"
  command: which php
  register: php_path

- name: "systemd"
  block:
    - name: "create systemd {{ mbqueue.files.systemd_production_service }}"
      template:
        src: "{{ mbqueue.files.systemd_production_service }}"
        dest: "{{ OS.systemd }}/{{ mbqueue.files.systemd_production_service }}"
        owner: root
        group: root
        mode: 0755

    - name: "create systemd {{ mbqueue.files.systemd_testing_service }}"
      template:
        src: "{{ mbqueue.files.systemd_testing_service }}"
        dest: "{{ OS.systemd }}/{{ mbqueue.files.systemd_testing_service }}"
        owner: root
        group: root
        mode: 0755

    - name: "reload systemd"
      command: systemctl daemon-reload
#    - name: "add {{ mbqueue.files.systemd_production_service }} to startup"
#      service:
#        name: "{{ mbqueue.files.systemd_production_service }}"
#        enabled: yes
#
#    - name: "run {{ mbqueue.files.systemd_production_service }}"
#      service:
#        name: "{{ mbqueue.files.systemd_production_service }}"
#        state: started
  when: php_path.stdout != ""