---
- name: "add cron file"
  template:
    src: "mbpayments.cron"
    dest: "{{ OS.cron.dirs.custom }}/mbpayments"
    owner: root
    group: root
    mode: u=rwx,g=rx,o=rx

- name: "restart {{ OS.cron.name }} service"
  service:
    name: "{{ OS.cron.name }}"
    state: restarted
