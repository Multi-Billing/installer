---
- name: "replace {{ mbcore.dirs.production }} cofig values"
  replace:
    path: "{{ billing.dirs.home }}/{{ mbcore.dirs.module }}/{{ mbcore.dirs.production }}/{{ mbcore.dirs.config }}/{{ mbcore.files.config }}"
    regexp: "{{ item.name }}"
    replace: "{{ item.value }}"
  with_items:
    - { name: '{server_host}', value: "{{ billing.mysql_server }}" }
    - { name: '{username}', value: "{{ mbcore.software.database.mysql_user }}" }
    - { name: '{database}', value: "{{ mysql.database }}" }
    - { name: '{log_general}', value: "{{ billing.dirs.home }}/{{ billing.dirs.logs }}/{{ mbcore.dirs.module }}/{{ mbcore.files.log_main }}" }
    - { name: '{log_debug}', value: "{{ billing.dirs.home }}/{{ billing.dirs.logs }}/{{ mbcore.dirs.module }}/{{ mbcore.files.log_debug }}" }
    - { name: '{timezone}', value: "{{ timezone.stdout }}" }
    - { name: '{kernel_pid}', value: "{{ mbcore.params.production.pid }}" }
    - { name: '{kernel_ip}', value: "{{ mbcore.params.production.ip }}" }
    - { name: '{kernel_port}', value: "{{ mbcore.params.production.port }}" }

- name: "replace mysql password"
  replace:
    path: "{{ billing.dirs.home }}/{{ mbcore.dirs.module }}/{{ mbcore.dirs.production }}/{{ mbcore.dirs.config }}/{{ mbcore.files.config }}"
    regexp: "{password}"
    replace: "{{ mbcore.software.database.mysql_pass }}"
  when: mbcore.software.database.mysql_pass != ""
  no_log: true

- name: "replace {{ mbcore.dirs.testing }} cofig values"
  replace:
    path: "{{ billing.dirs.home }}/{{ mbcore.dirs.module }}/{{ mbcore.dirs.testing }}/{{ mbcore.dirs.config }}/{{ mbcore.files.config }}"
    regexp: "{{ item.name }}"
    replace: "{{ item.value }}"
  with_items:
    - { name: '{server_host}', value: "{{ billing.mysql_server }}" }
    - { name: '{username}', value: "{{ mbcore.software.database.mysql_user }}" }
    - { name: '{database}', value: "{{ mysql.testing }}" }
    - { name: '{log_general}', value: "{{ billing.dirs.home }}/{{ billing.dirs.logs }}/{{ mbcore.dirs.module }}/{{ mbcore.files.log_main }}" }
    - { name: '{log_debug}', value: "{{ billing.dirs.home }}/{{ billing.dirs.logs }}/{{ mbcore.dirs.module }}/{{ mbcore.files.log_debug }}" }
    - { name: '{timezone}', value: "{{ timezone.stdout }}" }
    - { name: '{kernel_pid}', value: "{{ mbcore.params.testing.pid }}" }
    - { name: '{kernel_ip}', value: "{{ mbcore.params.testing.ip }}" }
    - { name: '{kernel_port}', value: "{{ mbcore.params.testing.port }}" }

- name: "replace mysql password"
  replace:
    path: "{{ billing.dirs.home }}/{{ mbcore.dirs.module }}/{{ mbcore.dirs.testing }}/{{ mbcore.dirs.config }}/{{ mbcore.files.config }}"
    regexp: "{password}"
    replace: "{{ mbcore.software.database.mysql_pass }}"
  when: mbcore.software.database.mysql_pass != ""
  no_log: true
