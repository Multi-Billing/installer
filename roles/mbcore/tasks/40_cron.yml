---
- name: "add cron file"
  template:
    src: "mbcore.cron"
    dest: "{{ OS.cron.dirs.custom }}/mbcore"
    owner: root
    group: root
    mode: u=rwx,g=rx,o=rx

- name: "restart {{ OS.cron.name }} service"
  service:
    name: "{{ OS.cron.name }}"
    state: restarted
