---
- name: "add cron file"
  template:
    src: "mbcabinet.cron"
    dest: "{{ OS.cron.dirs.custom }}/mbcabinet"
    owner: root
    group: root
    mode: u=rwx,g=rx,o=rx

- name: "restart {{ OS.cron.name }} service"
  service:
    name: "{{ OS.cron.name }}"
    state: restarted
