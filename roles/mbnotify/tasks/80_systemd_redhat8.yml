---
- name: "detect php"
  command: which php
  register: php_path

- name: "systemd"
  block:
    - name: "create systemd {{ mbnotify.files.systemd_production_service }}"
      template:
        src: "{{ mbnotify.files.systemd_production_service }}"
        dest: "{{ OS.systemd }}/{{ mbnotify.files.systemd_production_service }}"
        owner: root
        group: root
        mode: 0755

    - name: "create systemd {{ mbnotify.files.systemd_testing_service }}"
      template:
        src: "{{ mbnotify.files.systemd_testing_service }}"
        dest: "{{ OS.systemd }}/{{ mbnotify.files.systemd_testing_service }}"
        owner: root
        group: root
        mode: 0755

    - name: "reload systemd"
      command: systemctl daemon-reload
#    - name: "add {{ mbnotify.files.systemd_production_service }} to startup"
#      service:
#        name: "{{ mbnotify.files.systemd_production_service }}"
#        enabled: yes
#
#    - name: "run {{ mbnotify.files.systemd_production_service }}"
#      service:
#        name: "{{ mbnotify.files.systemd_production_service }}"
#        state: started
  when: php_path.stdout != ""