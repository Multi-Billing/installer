---
- name: "detect php"
  command: which php
  register: php_path

- name: "systemd"
  block:
    - name: "create systemd {{ mbmessenger.files.systemd_production_service }}"
      template:
        src: "{{ mbmessenger.files.systemd_production_service }}"
        dest: "{{ OS.systemd }}/{{ mbmessenger.files.systemd_production_service }}"
        owner: root
        group: root
        mode: 0755

    - name: "create systemd {{ mbmessenger.files.systemd_testing_service }}"
      template:
        src: "{{ mbmessenger.files.systemd_testing_service }}"
        dest: "{{ OS.systemd }}/{{ mbmessenger.files.systemd_testing_service }}"
        owner: root
        group: root
        mode: 0755

    - name: "reload systemd"
      command: systemctl daemon-reload
#    - name: "add {{ mbmessenger.files.systemd_production_service }} to startup"
#      service:
#        name: "{{ mbmessenger.files.systemd_production_service }}"
#        enabled: yes
#
#    - name: "run {{ mbmessenger.files.systemd_production_service }}"
#      service:
#        name: "{{ mbmessenger.files.systemd_production_service }}"
#       state: started
  when: php_path.stdout != ""