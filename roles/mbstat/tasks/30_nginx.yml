---
- name: "create {{ mbstat.dirs.module }} hosts"
  template:
    src: "{{ item }}"
    dest: "{{ OS.nginx.dirs.conf }}/conf.d/{{ item }}"
    mode: u=rw,g=r,o=r
  with_items:
    - "mbstat_vhost_production.conf"
    - "mbstat_vhost_testing.conf"

- name: "restart {{ OS.nginx.name }} service"
  service:
    name: "{{ OS.nginx.name }}"
    state: restarted
